CREATE TABLE CryptoPriceDBO(
    id INTEGER AS Int NOT NULL PRIMARY KEY,
    price REAL NOT NULL,
    priceChange REAL NOT NULL,
    priceChangePercents REAL NOT NULL
);

insert:
INSERT OR REPLACE INTO CryptoPriceDBO VALUES ?;

getAll:
SELECT * FROM CryptoPriceDBO;